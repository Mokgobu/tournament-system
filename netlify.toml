[build]
  functions = "netlify/functions"
  publish = "src"

[dev]
  functions = "netlify/functions"
  publish = "src"
  autoLaunch = true

# Only this one redirect - remove any others
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200